<div class="fixed-width-form">
  <% if (locals.notice) { %>
    <div className="notice"><%= notice %></div>
  <% } %>
  <h1>Inicio de Sesión</h1>
  <br>
  <% if (locals.error) { %>
    <div class="error">
      <%= error %>
    </div>
  <% } %>
  <form action="<%= createSessionPath %>" method="post" class="border form">
    <input type="hidden" name="_method" value="put" />
    <div class="field">
      <label for="mail">Correo electrónico</label>
      <input type="mail" name="mail" value="<%= locals.email %>"  class="float-r"/>
    </div>
    <br>
    <div class="field">
      <label for="password">Contraseña</label>
      <input type="password" name="password" value="<%= locals.password %>" class="float-r"/>
    </div>
    <br>
    <div class="field">
      <input type="submit" name="sign-in" value="Iniciar Sesión" class="btn"/>
    </div>
  </form>
</div>
