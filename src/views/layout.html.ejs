<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>IIC2513 - X-Change</title>
    <% if (env !== 'development') { %>
      <link rel="stylesheet" href="<%= assetPath('app.css') %>">
    <% } %>
  </head>

  <body>
    <header class="flexbox-container">
      <div class="flexbox-item home">
        <a href="/">Home</a>
        <a href="#">Search</a>
      </div>

      <div class="flexbox-item title">
        <h1>Grupo 39, el mas bacan</h1>
      </div>

      <!-- if not logged in -->
      <div class="flexbox-item session flexbox-container">
        <div>
          <a href="#">Log In</a>
        </div>
        <div>
          <a href="#">Register</a>
        </div>
      </div>

      <!-- if logged in -->
      <div class="hidden">
        <div>
          <a href="#">Messages</a>
        </div>
        <div>
          <a href="#">Account</a>
        </div>
      </div>

    </header>
    <%- body %>
    <script src="<%= assetPath('app.js') %>"></script>
  </body>

</html>
