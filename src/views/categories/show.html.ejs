<div class="fixed-width-lg horizontal-center-container">
  <h1 class="mv"> Artículos de la categoría <%= category.name %> </h1>
  <div class="form mv">
    <div id="page">
      <% if (!objectsList.length) { %>
        <div class="group">
          <a href="javascript:void(0)" class="group-item active">
            <h4 class="group-item-heading">No hay objetos de esta categoría.</h4>
            </a>
          </div>
        <% } else { %>
          <% objectsList.forEach((object) => { %>
        <div class="group">
          <a href="<%= viewObjectPath(object) %>" class="group-item active">
            <span class="float-l cursiva span-image-search"><img class="navbar-images" src="https://xchangestorage.s3.us-east-2.amazonaws.com/account.png"><span><%= object.user.username %></span></span>
            <span class="float-r cursiva span-image-search"><img class="navbar-images" src="https://xchangestorage.s3.us-east-2.amazonaws.com/address.png"><span><%= object.user.region %></span></span>
            <h3 class="underline"><%= object.name %></h3>
            <p class="group-item-text max-lines"><%= object.description %></p>
          </a>
        </div>
        <% }) %>
        <% } %>
      </div>
      <nav aria-label=...>
        <ul class=pagination>
          <li id="previous-page"><a href="javascript:void(0)" aria-label=Previous><span aria-hidden=true>&laquo;</span></a></li>
        </ul>
      </nav>
    </div>
    <a class="btn mv" href="/">Volver</a>
</div>
